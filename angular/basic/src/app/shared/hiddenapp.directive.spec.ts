import { HiddenappDirective } from './hiddenapp.directive';

describe('HiddenappDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenappDirective();
    expect(directive).toBeTruthy();
  });
});
